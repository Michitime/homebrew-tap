# Michitime Homebrew Tap

Install WatsonMenuBar:

```sh
brew tap Michitime/tap
brew install --cask watson-menu-bar
```

Or install it directly:

```sh
brew install --cask Michitime/tap/watson-menu-bar
```
